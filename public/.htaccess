RewriteEngine On

# Se existir arquivo/pasta física, não reescreve
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Front controller local da pasta public
RewriteRule ^ index.php [L,QSA]

